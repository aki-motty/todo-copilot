#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

echo "🔍 Running pre-commit checks..."

# 1. Run lint-staged (format & lint)
echo "📝 Checking staged files with Biome..."
npx lint-staged

# 2. Type check
echo "🔧 Running type check..."
npm run type-check

# 3. Run unit tests (affected tests only for speed)
echo "🧪 Running tests..."
npm test -- --passWithNoTests --bail

echo "✅ All pre-commit checks passed!"
